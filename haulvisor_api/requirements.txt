# -------------------------------
# haulvisor_api/requirements.txt
# -------------------------------

# ── Web framework & server ──────────────────────────────────────────────────────
fastapi>=0.110,<0.120        # tracks current 0.110.x series, blocks a future 1.0 jump
uvicorn[standard]>=0.29,<0.40

# ── Validation & settings ───────────────────────────────────────────────────────
pydantic>=2.7,<3             # satisfies HaulVisor (>=2.7) and FastAPI (<3)
pydantic-settings>=2.3,<3
python-multipart             # needed for multipart/form-data uploads

# ── CLI / logging / pretty output ───────────────────────────────────────────────
typer
rich
loguru

# ── Quantum SDKs & back-ends ────────────────────────────────────────────────────
pennylane                    # latest compatible release will be chosen
qiskit
qiskit-aer
qiskit-ibm-runtime
amazon-braket-sdk
pennylane-qiskit

# ── AWS helpers ─────────────────────────────────────────────────────────────────
boto3

# ── Numeric / scientific stack ─────────────────────────────────────────────────
numpy                         # leave un-pinned to let SDKs pick a compatible build
scipy
PyYAML

