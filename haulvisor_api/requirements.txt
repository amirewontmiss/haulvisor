# ── Web framework & server ─────────────────────────────────────────────
fastapi>=0.110,<0.120
uvicorn[standard]>=0.29,<0.40

# ── Validation & settings ──────────────────────────────────────────────
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
python-multipart

# ── CLI / logging / pretty output ──────────────────────────────────────
typer
rich
loguru

# ── Quantum SDKs & back-ends ───────────────────────────────────────────
pennylane==0.41.1
pennylane-qiskit==0.41.0.post0      # provides qml.from_qasm plugin

# --->  ⬇  stick to the legacy 0.x line ⬇  <---
qiskit==0.46.3
qiskit-aer==0.13.3
qiskit-ibm-runtime==0.14.0
# --------------------------------------------

amazon-braket-sdk                   # optional – leave unpinned
amazon-braket-pennylane-plugin      # optional – leave unpinned

# ── AWS helpers ────────────────────────────────────────────────────────
boto3

# ── Numeric / scientific stack ─────────────────────────────────────────
numpy
scipy
PyYAML

