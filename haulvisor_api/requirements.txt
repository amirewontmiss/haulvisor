# --------------------------------------------------------------------------
# haulvisor_api/requirements.txt
# --------------------------------------------------------------------------
# ── API + server ───────────────────────────────────────────────────────────
fastapi>=0.110,<0.120
uvicorn[standard]>=0.29,<0.40

# ── Validation & settings ─────────────────────────────────────────────────
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
python-multipart

# ── CLI / logging / UX ────────────────────────────────────────────────────
typer
rich
loguru

# ── Quantum SDKs & plug-ins ───────────────────────────────────────────────
pennylane>=0.41                 # core framework
pennylane-qiskit>=0.41          # QASM loader plugin used by HaulVisor
qiskit==0.46.3                  # match the plugin’s tested range
qiskit-aer==0.13.3
qiskit-ibm-runtime==0.21.0      # contains SamplerV2 → avoids ImportError
amazon-braket-sdk               # optional AWS back-end
amazon-braket-pennylane-plugin  # PennyLane-to-Braket connector (optional)

# ── AWS helpers ───────────────────────────────────────────────────────────
boto3

# ── Numeric / scientific stack ────────────────────────────────────────────
numpy                            # left unpinned for wheel compatibility
scipy
PyYAML

